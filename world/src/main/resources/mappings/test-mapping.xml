<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 마이바티스에 사용할 db sql 문을 저장하는데 사용하는 xml 파일이다. -->

<mapper namespace="TestDAO">

	<insert id="insertTest">
		insert into Test(testId, name, pwd)
		values((select nvl(max(testId), 0)+1 from Test),#{name},#{pwd})
	</insert>

	<update id="updateTest">
		update Test set name=#{name}, pwd=#{pwd} where testId=#{testId}
	</update>
	
	<delete id="deleteTest">
		delete Test where testId=#{testId}
	</delete>
	
	<select id="getTest" resultType="Test">
		select * from Test where testId = #{testId}
	</select>
	
	<select id="getTestList" resultType="Test">
		select * from Test 
		where name like '%'||#{searchKeyword}||'%'
		order by testId desc
	</select>
	
</mapper>
